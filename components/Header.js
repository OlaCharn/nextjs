import Heading from "@/components/Heading";
import Navbar from "./Navbar";

const Header = () => {
    return(
        <header>
            <Navbar/>
        </header>
    )
    }
export default Header;